extends ../layouts/main-layout.pug

include ../mixins/createList.pug
block styles
    link(rel="stylesheet" href="/css/forms.css")

block content
    .row
        .col-md-9.pl-3
            if users.length > 0
                    each user in users
                        +createList(user)
            else
                h1 No users
                        

